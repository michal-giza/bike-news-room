## Rust
target/

## SQLite — runtime artefacts only
*.db
*.db-journal
*.db-wal
*.db-shm

## Secrets
.env
.env.*
!.env.example

## OS
.DS_Store
Thumbs.db

## Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
**/.dart_tool/
**/build/

## Editor / IDE
.idea/
.vscode/
*.iml
*.swp
*.swo

## Cloudflare scratch — only used by the build script
_flutter/

## Local-only artefacts that shouldn't leak
*.log
*.tmp

## Claude Code session-local state (not project content)
.claude/

# Local debugging artifacts (Flutter/adb capture sessions)
logs.md
*.logcat

# Android release-signing — NEVER commit. The keystore + password
# unlock Play Console uploads for this app id; treat as a secret.
key.properties
local.properties
upload-keystore.jks
*.jks
*.keystore
